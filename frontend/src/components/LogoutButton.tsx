const LogoutButton = () => {
  return (
    <button className="px-3 py-2 bg-white text-primarySoft text-sm font-semibold flex items-center hover:bg-blue-50 rounded-sm">
      Logout
    </button>
  );
};

export default LogoutButton;
